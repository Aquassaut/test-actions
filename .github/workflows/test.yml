name: playground

on:
  push:
    branches: [ "main" ]

jobs:
  reusable-workflow:
    strategy:
      matrix:
        environment: [env1, env2]
    uses: ./.github/workflows/workflow.yml
    with:
      environment: ${{ matrix.environment }}
